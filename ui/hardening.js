// Minimal fetch hardening shim (kept very small; index.html does the heavy lifting).
// Intentionally no global overrides here to avoid double-wrapping.
(()=>{
  // Placeholder for future CSP/nonce work.
})();
